---
phase: 04-security-new-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blend_ai/tools/materials.py
  - src/blend_ai/tools/uv.py
  - src/blend_ai/tools/rendering.py
  - addon/handlers/uv.py
  - addon/handlers/rendering.py
  - tests/test_tools/test_materials.py
  - tests/test_tools/test_uv.py
  - tests/test_tools/test_rendering.py
autonomous: true
requirements:
  - NEW51-01
  - NEW51-05
  - NEW51-06

must_haves:
  truths:
    - "SLIM is accepted as a valid unwrap method in uv_unwrap tool"
    - "New 5.1 shader node types are in the ALLOWED_SHADER_NODE_TYPES allowlist"
    - "EEVEE light path intensity properties are configurable via set_render_property or a new tool"
  artifacts:
    - path: "src/blend_ai/tools/uv.py"
      provides: "SLIM unwrap method in ALLOWED_UNWRAP_METHODS"
      contains: "SLIM"
    - path: "src/blend_ai/tools/materials.py"
      provides: "New 5.1 shader nodes in ALLOWED_SHADER_NODE_TYPES"
      contains: "ShaderNodeRaycast"
---

<objective>
Add Blender 5.1-exclusive features: SLIM UV unwrap method, new shader node types, and EEVEE light path intensity controls.

Purpose: Users on Blender 5.1 should have access to all new features through MCP tools without needing to use code_exec.

Output: Updated allowlists, new handler support, and tests.
</objective>

<context>
@.planning/phases/04-security-new-tools/04-RESEARCH.md
@src/blend_ai/tools/materials.py
@src/blend_ai/tools/uv.py
@src/blend_ai/tools/rendering.py
@addon/handlers/uv.py
@addon/handlers/rendering.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SLIM unwrap method and new shader nodes</name>
  <files>src/blend_ai/tools/uv.py, src/blend_ai/tools/materials.py, tests/test_tools/test_uv_slim.py, tests/test_tools/test_new_nodes.py</files>
  <read_first>
    - src/blend_ai/tools/uv.py (ALLOWED_UNWRAP_METHODS)
    - src/blend_ai/tools/materials.py (ALLOWED_SHADER_NODE_TYPES)
    - addon/handlers/uv.py (existing handler)
  </read_first>
  <behavior>
    - Test: "SLIM" is in ALLOWED_UNWRAP_METHODS
    - Test: uv_unwrap(method="SLIM") passes validation (doesn't raise ValidationError)
    - Test: "ShaderNodeRaycast" is in ALLOWED_SHADER_NODE_TYPES
    - Test: add_shader_node(node_type="ShaderNodeRaycast") passes validation
  </behavior>
  <action>
    1. Add "SLIM" to ALLOWED_UNWRAP_METHODS in src/blend_ai/tools/uv.py
    2. Add new 5.1 shader nodes to ALLOWED_SHADER_NODE_TYPES in src/blend_ai/tools/materials.py:
       - "ShaderNodeRaycast" (Raycast node for shader trees)
    3. Write tests verifying the allowlist additions
  </action>
  <verify>
    <automated>cd /Users/michael/code/blend-ai && uv run pytest tests/test_tools/test_uv_slim.py tests/test_tools/test_new_nodes.py -x -v</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EEVEE light path intensity controls</name>
  <files>src/blend_ai/tools/rendering.py, addon/handlers/rendering.py, tests/test_tools/test_eevee_light_path.py</files>
  <read_first>
    - src/blend_ai/tools/rendering.py (existing rendering tools)
    - addon/handlers/rendering.py (existing handlers)
  </read_first>
  <behavior>
    - Test: set_eevee_light_path tool validates intensity values (0.0-10.0)
    - Test: set_eevee_light_path sends correct command with parameters
    - Test: set_eevee_light_path supports diffuse, glossy, transmission intensity properties
  </behavior>
  <action>
    1. Add set_eevee_light_path MCP tool in rendering.py
    2. Add handle_set_eevee_light_path handler in rendering.py handler
    3. Write tests
  </action>
  <verify>
    <automated>cd /Users/michael/code/blend-ai && uv run pytest tests/test_tools/test_eevee_light_path.py -x -v</automated>
  </verify>
</task>

</tasks>

<verification>
- uv run pytest tests/ -x (full suite)
</verification>

<success_criteria>
- SLIM unwrap method accepted
- New 5.1 shader nodes in allowlist
- EEVEE light path intensity configurable
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-new-tools/04-02-SUMMARY.md`
</output>
