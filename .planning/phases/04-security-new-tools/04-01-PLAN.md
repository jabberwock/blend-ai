---
phase: 04-security-new-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - addon/handlers/code_exec.py
  - src/blend_ai/tools/code_exec.py
  - tests/test_addon/test_handlers/test_code_exec_handler.py
  - tests/test_tools/test_code_exec.py
autonomous: true
requirements:
  - STAB-03
  - NEW51-07

must_haves:
  truths:
    - "Executing `import os` in code_exec raises ImportError with 'blocked for security' message"
    - "Executing `import subprocess` in code_exec raises ImportError"
    - "Executing `import bpy` in code_exec works (allowed)"
    - "Executing `import math` in code_exec works (allowed)"
    - "eval() and exec() are not available in sandboxed code"
    - "open() is not available in sandboxed code"
    - "The sandbox is the default mode (safe by default)"
  artifacts:
    - path: "addon/handlers/code_exec.py"
      provides: "Sandboxed code execution with BLOCKED_MODULES and SAFE_BUILTINS"
      contains: "BLOCKED_MODULES"
    - path: "tests/test_addon/test_handlers/test_code_exec_handler.py"
      provides: "Tests verifying sandbox blocks dangerous imports"
      contains: "test_blocks_os_import"
    - path: "tests/test_tools/test_code_exec.py"
      provides: "Tests for MCP tool layer"
      contains: "test_empty_code_raises"
---

<objective>
Sandbox the code_exec handler to prevent arbitrary code execution (RCE) by blocking dangerous imports and restricting builtins.

Purpose: The current code_exec handler passes unrestricted `__builtins__` to `exec()`, allowing any Python code including `import os; os.system("rm -rf /")`. This is the only known security vulnerability in the project.

Output: Sandboxed handler with blocked module list, restricted builtins, and comprehensive security tests.
</objective>

<execution_context>
@.planning/phases/04-security-new-tools/04-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-security-new-tools/04-RESEARCH.md
@addon/handlers/code_exec.py
@src/blend_ai/tools/code_exec.py
@tests/test_addon/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for sandboxed code execution</name>
  <files>tests/test_addon/test_handlers/test_code_exec_handler.py, tests/test_tools/test_code_exec.py</files>
  <read_first>
    - addon/handlers/code_exec.py (current unsandboxed handler)
    - tests/test_addon/test_handlers/test_rendering_handler.py (importlib loading pattern)
    - tests/test_addon/conftest.py (mock bpy)
    - src/blend_ai/tools/code_exec.py (current MCP tool)
    - tests/test_tools/conftest.py (mock server)
  </read_first>
  <behavior>
    Handler tests:
    - Test: `import os` in code raises RuntimeError containing "blocked" or "security"
    - Test: `import subprocess` in code raises RuntimeError containing "blocked"
    - Test: `import socket` in code raises RuntimeError containing "blocked"
    - Test: `import shutil` in code raises RuntimeError containing "blocked"
    - Test: `__import__("os")` in code raises RuntimeError containing "blocked"
    - Test: `exec("print(1)")` in code raises RuntimeError (exec not available)
    - Test: `eval("1+1")` in code raises RuntimeError (eval not available)
    - Test: `open("/etc/passwd")` in code raises RuntimeError (open not available)
    - Test: `import bpy` in code succeeds (allowed)
    - Test: `import math` in code succeeds (allowed)
    - Test: `import json` in code succeeds (allowed)
    - Test: `print("hello")` in code works and output captured
    - Test: Valid code with result returns {"output": ..., "success": True}
    
    MCP tool tests:
    - Test: Empty code raises ValueError
    - Test: Whitespace-only code raises ValueError
    - Test: Valid code sends execute_code command
    - Test: Error response raises RuntimeError
  </behavior>
  <action>
    Create both test files following existing patterns. Handler tests use importlib direct-loading.
    MCP tool tests use mock_conn fixture.
  </action>
  <verify>
    <automated>cd /Users/michael/code/blend-ai && uv run pytest tests/test_addon/test_handlers/test_code_exec_handler.py tests/test_tools/test_code_exec.py -x -v 2>&1 | tail -30</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sandboxed code execution handler (GREEN)</name>
  <files>addon/handlers/code_exec.py</files>
  <read_first>
    - addon/handlers/code_exec.py (rewrite in place)
    - tests/test_addon/test_handlers/test_code_exec_handler.py (tests to make pass)
  </read_first>
  <behavior>
    Make all handler tests pass. Blocked modules raise ImportError. Safe builtins exclude exec/eval/open/etc.
  </behavior>
  <action>
    Rewrite addon/handlers/code_exec.py with:
    1. BLOCKED_MODULES set
    2. _build_safe_builtins() function
    3. _safe_import() function
    4. Updated handle_execute_code using restricted exec
  </action>
  <verify>
    <automated>cd /Users/michael/code/blend-ai && uv run pytest tests/test_addon/test_handlers/test_code_exec_handler.py tests/test_tools/test_code_exec.py -x -v</automated>
  </verify>
</task>

</tasks>

<verification>
- uv run pytest tests/test_addon/test_handlers/test_code_exec_handler.py tests/test_tools/test_code_exec.py -x -v
- uv run pytest tests/ -x (full suite)
</verification>

<success_criteria>
- import os/subprocess/socket/shutil blocked with clear error
- import bpy/math/json/bmesh allowed
- exec/eval/open/compile not in sandboxed builtins
- All tests pass, full suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-new-tools/04-01-SUMMARY.md`
</output>
